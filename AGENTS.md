## Agent skills

### Issue tracker

Issue は GitHub Issues で管理し、操作には `gh` CLI を使う。詳細は `docs/agents/issue-tracker.md` を参照する。

### Domain docs

single-context 構成として、ルートの `CONTEXT.md` と `docs/adr/` を参照する。詳細は `docs/agents/domain.md` を参照する。
